// Kích thước lưới tính bằng px, dùng chung cho header giờ và các dòng sân để hai phần luôn khớp cột.
export const scheduleLayout = {
  courtColumnWidth: 78,
  groupColumnWidth: 54,
  rowHeight: 44,
  slotWidth: 48,
  timeHeaderHeight: 28,
};

export const leadColumnWidth = scheduleLayout.groupColumnWidth + scheduleLayout.courtColumnWidth;
